#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
int main(void)
{
	int a[101][101];
	int n, m,i,j;
	scanf("%d %d", &n, &m);
	
	for (i = 1; i <= n; i++)
	{
		for (j = 1; j <= m; j++)
		{
			scanf("%d", &a[i][j]);
		}
	}

	int sum = 0;
	
	
}